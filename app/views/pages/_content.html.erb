
<!-- Page Content  -->
<div id="content">
  <!-- logo -->
  <div class="d-flex justify-content-center">
    <%= image_tag("logo_genx", height: '100', width: '100') %>
  </div>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <!-- Collapse sidebar button -->
            <button type="button" id="sidebarCollapse" class="btn btn-info">
                <i class="fas fa-align-left"></i>
                <span></span>
            </button>
            <!-- Filter button -->
            <%= link_to "Offer an experience!", "#", class: "btn btn-ghost" %>
            <div>
                <ul class="nav navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="#"><i class="fas fa-filter"></i></a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <!-- The Cards -->
    <% @experiences.each do |experience| %>
      <%= link_to experience_path(experience), class: "text-decoration-none" do %>
      <div class="notification">
        <div class="notification-content col-2">
          <% if experience.gender == "Male" %>
             <%= image_tag("old_man") %>
          <% else %>
              <%= image_tag("old_woman") %>
          <% end %>
        </div>
        <div class="notification-content col-3">
          <p><%= "#{experience.first_name} #{experience.last_name}" %></p>
        </div>
        <div class="notification-content col-2">
          <p><%= "#{experience.age} years" %></p>
        </div>
        <div class="notification-content col-3">
          <p>Language</p>
        </div>
        <div class="notification-content col-2">
          <p>Skill</p>
        </div>
      </div>
      <% end %>
    <% end %>
    <div class="line"></div>

</div>
